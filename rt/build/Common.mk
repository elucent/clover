# Build directories and goals

BUILD := bin
BUILD_CONFIG := $(BUILD)/config
DEBUG_BUILD := $(BUILD)/debug
RELEASE_BUILD := $(BUILD)/release
DEBUG_MANIFEST := $(DEBUG_BUILD)/manifest
RELEASE_MANIFEST := $(RELEASE_BUILD)/manifest
